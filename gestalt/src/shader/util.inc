float saturate(const in float i) {
    return clamp(i, 0.0, 1.0);
}
vec2 saturate(const in vec2 i) {
    return clamp(i, 0.0, 1.0);
}
vec3 saturate(const in vec3 i) {
    return clamp(i, 0.0, 1.0);
}
vec4 saturate(const in vec4 i) {
    return clamp(i, 0.0, 1.0);
}
