extern crate std;
use std::ops::{Add, Sub, Mul, Div};
use std::cmp::{Ord, Eq};
use std::string::String;
use std::vec::Vec;
use voxel::coord3::Coord3;
use voxel::voxelstorage::VoxelStorage;

pub fn render(VoxelStorage vox) {
	
}